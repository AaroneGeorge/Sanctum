# Point the phone app at the Sanctum inference server running on your laptop.
# Must be the laptop's LAN IP (NOT localhost) and the phone must be on the SAME Wi-Fi.
# Find it from the server's startup log ("LAN: http://<ip>:8787") or `ip addr`.
EXPO_PUBLIC_SERVER_URL=
