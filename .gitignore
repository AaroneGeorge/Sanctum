node_modules/
# Model weights are large; download via scripts/download-models.sh (do NOT commit the GGUFs).
models/*.gguf
# Keep the checksum manifest committed for reproducibility:
!models/SHA256SUMS.txt
# Generated run artifacts are regenerated by the demo; keep the committed reference copies only.
artifacts/*.jsonl
artifacts/profiler.json
*.log
.DS_Store
